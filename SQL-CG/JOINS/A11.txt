SQL> --WAQTD NAME OF THE EMPLOYEE EARNING MAXIMUM SALARY:
SQL> SELECT ENAME
  2  FROM EMP
  3  WHERE SAL=(SELECT MAX(SAL) FROM EMP);

ENAME
----------
KING

SQL> --NAME OF THE EMPLOYEE EARNING MINIMUM SALARY:
SQL> SELECT ENAME
  2  FROM EMP
  3  WHERE SAL=(SELECT MIN(SAL) FROM EMP);

ENAME
----------
SMITH

SQL> --NAME AND HIREDATE OF THE EMPLOYEE HIRED BEFORE ALL THE EMP(FIRST EMP):
SQL> SELECT ENAME, HIREDATE
  2  FROM EMP
  3  WHERE HIREDATE=(SELECT MIN(HIREDATE) FROM EMP);

ENAME      HIREDATE
---------- ---------
SMITH      17-DEC-80

SQL> --NAME AND HIREDATE OF THE EMPLOYEES HIRED AT THE LAST:
SQL> SELECT ENAME,HIREDATE
  2  FROM EMP
  3  WHERE HIREDATE=(SELECT MAX(HIREDATE) FROM EMP);

ENAME      HIREDATE
---------- ---------
ADAMS      23-MAY-87

SQL> --WAQTD NAME, COMM OF THE EMPLOYEE WHO EARNS MIN COMISSION:
SQL> SELECT ENAME,COMM
  2  FROM EMP
  3  WHERE COMM=(SELECT MIN(COMM) FROM EMP);

ENAME            COMM
---------- ----------
TURNER              0

SQL> --WAQTD NAME, SAL AND COMM OF THE EMPLOYEE EARNING MAX COMM:
SQL> SELECT ENAME,COMM
  2  FROM EMP
  3  WHERE COMM=(SELECT MAX(COMM) FROM EMP);

ENAME            COMM
---------- ----------
MARTIN           1400

SQL> --WAQTD DETAILS OF THE EMPLOYEE WHO HAS GREATEST EMPNO:
SQL> SELECT *
  2  FROM EMP
  3  WHERE EMPNO=(SELECT MAX(EMPNO) FROM EMP);

     EMPNO ENAME      JOB              MGR HIREDATE         SAL       COMM     DEPTNO
---------- ---------- --------- ---------- --------- ---------- ---------- ----------
      7934 MILLER     CLERK           7782 23-JAN-82       1300                    10

SQL> --WAQTD DETAILS OF THE EMPLOYEES HAVING THE LEAST HIREDATE:
SQL> SELECT *
  2  FROM EMP
  3  WHERE EMPNO=(SELECT MIN(HIREDATE) FROM EMP);
WHERE EMPNO=(SELECT MIN(HIREDATE) FROM EMP)
           *
ERROR at line 3:
ORA-00932: inconsistent datatypes: expected NUMBER got DATE


SQL> SELECT *
  2  FROM EMP
  3  WHERE EMPNO IN (SELECT MIN(HIREDATE) FROM EMP);
WHERE EMPNO IN (SELECT MIN(HIREDATE) FROM EMP)
            *
ERROR at line 3:
ORA-00932: inconsistent datatypes: expected NUMBER got DATE


SQL> SELECT *
  2  FROM EMP
  3  WHERE HIREDATE= (SELECT MIN(HIREDATE) FROM EMP);

     EMPNO ENAME      JOB              MGR HIREDATE         SAL       COMM     DEPTNO
---------- ---------- --------- ---------- --------- ---------- ---------- ----------
      7369 SMITH      CLERK           7902 17-DEC-80        800                    20

SQL> --WAQTD DETAILS OF THE EMPLOYEES EARNING LEAST ANNUAL SAL:
SQL> SELECT *
  2  FROM EMP
  3  WHERE SAL*12=(SELECT MIN(SAL*12) FROM EMP);

     EMPNO ENAME      JOB              MGR HIREDATE         SAL       COMM     DEPTNO
---------- ---------- --------- ---------- --------- ---------- ---------- ----------
      7369 SMITH      CLERK           7902 17-DEC-80        800                    20

SQL> --WAQTD NAME ANNUAL SALARY OF THE EMPLOYEES IF THEIR ANNUAL SALARY IS MORE THAN ALL THE SALESMAN:
SQL> SELECT ENAME,SAL
  2  FROM EMP
  3  WHERE SAL*12=(SELECT MAX(SAL*12) FROM EMP WHERE JOB='SALESMAN');

ENAME             SAL
---------- ----------
ALLEN            1600

SQL> SPOOL OFF